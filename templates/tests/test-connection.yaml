apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gnes-helm.fullname" . }}-test-connection"
  labels:
{{ include "gnes-helm.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "gnes-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
